name: harry_potter_app1
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  grock: ^0.3.3
  dio:
  flutter_riverpod:
  json_annotation:
  freezed_annotation:
  cupertino_icons: ^1.0.6
  flutter_launcher_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner:
  json_serializable:
  freezed:


flutter:
  assets:
    - assets/image/
    - assets/icon/

uses-material-design: true

flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/image/application_logo.png"

